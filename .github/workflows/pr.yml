name: PR
on: [pull_request]

jobs:
  pr:
    strategy:
      matrix:
        step: ['lint', 'compile', 'build:types']
    name: ${{ matrix.step }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v1
      
      - name: Type definitions ${{ matrix.step }}
        run: |
          cd pacakges/type-definitions
          yarn
          yarn ${{ matrix.step }}
      
      - name: Crust pin ${{ matrix.step }}
        run: |
          cd packages/crust-pin
          yarn
          yarn ${{ matrix.step }}

      