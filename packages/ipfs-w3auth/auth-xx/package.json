{
  "name": "@crustio/ipfs-w3auth-xx",
  "version": "0.0.3",
  "description": "XX network authentication of IPFS W3Auth",
  "main": "./src/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "build:types": "npx ts-node src/scripts/generate.ts",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "pub": "tsc && copyfiles ./pacakge.json ./build && cd build && npm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "copyfiles": "latest",
    "gts": "^3.1.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "axios": "^0.27.2"
  }
}
