{
  "name": "@crustnetwork/ipfs-w3auth-flow",
  "version": "0.0.1",
  "description": "Flow authentication of IPFS W3Auth",
  "main": "./src/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "build:types": "npx ts-node src/scripts/generate.ts",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "pub": "tsc && copyfiles ./package.json ./build && cd build && npm publish --access public"
  },
  "dependencies": {
    "@onflow/fcl": "0.0.77",
    "@onflow/types": "0.0.5"
  },
  "devDependencies": {
    "copyfiles": "latest",
    "@babel/runtime": "^7.13.2",
    "@types/node": "^14.14.31",
    "gts": "^3.1.0",
    "typescript": "^4.1.3"
  }
}
